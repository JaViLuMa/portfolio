import React, { FC } from 'react';

import { Title } from '../';

import './index.css';

const Wrapper:FC = () => {
  return (
    <div className="main-container">
      <Title />
    </div>
  );
}

export default Wrapper;
